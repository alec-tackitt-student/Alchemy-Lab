using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Escape : MonoBehaviour
{
    public static bool hasTriedEscape = false;
    // Start is called before the first frame update
    void Start()
    {
        hasTriedEscape = false;
    }

    
  
}
